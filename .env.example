# Webflow origin URL - set this to your Webflow site
# Examples:
#   # The URL of your Webflow site (origin)
WEBFLOW_ORIGIN=https://your-site.webflow.io

# Optional: The mount path of the app (defaults to empty for root, or read from COSMIC_MOUNT_PATH)
# MOUNT_PATH=/preview

# "Preview Mode" - Set to 'true' if you mount at /preview but want to fetch the root (/) content
# PROXY_TARGET_ROOT=true
#   WEBFLOW_ORIGIN=https://www.your-custom-domain.com
WEBFLOW_ORIGIN=
